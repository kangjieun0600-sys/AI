# 사이트 구조 문서

## 목적
**여러** 반려견을 소개하는 사이트로 변경. 프레임워크 없이 순수 HTML/CSS/JS로 제작하되, 상단 히어로에는
스와이퍼(이미지 캐러셀) 기능을 넣는다. 배경은 흰색 기반의 심플한 디자인으로, 가독성/가시성을 우선한다.
이미지는 Unsplash를 사용한다 (직접 링크, 별도 다운로드 없이 hotlink — Unsplash는 이런 사용을 허용함).

## 소개하는 강아지 (5마리)

| 이름 | 견종 | 나이 | 한줄 성격 |
|---|---|---|---|
| 코코 | 포메라니안 | 3살 | 애교쟁이 |
| 몽치 | 진돗개 | 2살 | 씩씩함 |
| 두부 | 말티즈 | 1살 | 장난꾸러기 |
| 초코 | 푸들 | 4살 | 차분함 |
| 보리 | 웰시코기 | 2살 | 활발함 |

## 사이트맵 (4페이지, 기존 구조 유지)

| 페이지 | 파일명 | 목적 |
|---|---|---|
| 홈 | `index.html` | 상단 스와이퍼(5마리 사진 슬라이드) + 소개 + 섹션 미리보기 카드 |
| 프로필 | `profile.html` | 5마리 각각의 프로필 카드(사진/이름/견종/나이/성격 태그) |
| 사진첩 | `gallery.html` | 전체 강아지 사진 그리드 (이름 캡션 포함) |
| 일상·추억 | `diary.html` | 강아지별 하루 일과/추억 에피소드 |

## 폴더 구조

```
web/
├── structure.md
├── index.html
├── profile.html
├── gallery.html
├── diary.html
├── css/
│   └── style.css
├── js/
│   ├── include.js    # 공통 헤더/푸터 삽입 (기존 재사용)
│   └── main.js        # 모바일 메뉴 토글 + 스와이퍼 초기화
└── components/
    ├── header.html
    └── footer.html
```

## 디자인 방향 (변경)

- **배경**: 흰색(`#ffffff`) 고정, 이전의 따뜻한 크림/오렌지 톤 제거
- **포인트 컬러**: 본문 대비가 잘 되는 진한 톤 하나만 (버튼/링크/태그용)
- **가독성 우선**: 본문 텍스트는 연한 회색(muted) 남용하지 않고 충분한 대비 유지, 폰트 크기와 줄간격 넉넉하게
- **여백**: 섹션 간 여백을 넉넉히 둬서 화면이 복잡해 보이지 않게 함

## 히어로 스와이퍼

라이브러리 없이 직접 구현하지 않고 **Swiper.js**(jsDelivr CDN)를 사용한다 — 터치 스와이프, 자동재생,
페이지네이션 점을 직접 구현하는 것보다 안정적이다.

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css">
<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
```

- 슬라이드 5장 = 소개하는 강아지 5마리 각각의 대표 사진 + 이름 오버레이
- 자동재생 + 하단 페이지네이션 점 + 좌우 스와이프/드래그 지원
- 초기화 코드는 `js/main.js`에 둔다 (헤더가 fetch로 삽입된 뒤 실행되어야 하는 다른 로직과 같은 타이밍에)

## 이미지 (Unsplash)

사전에 `curl`로 실제 접근 가능한지 확인한 강아지 사진 12장을 확보해 각 페이지에 배분한다.
`https://images.unsplash.com/photo-<id>?auto=format&fit=crop&w=<가로px>&q=60` 형식으로 사용하고,
용도에 따라 `w` 값만 다르게 준다 (히어로는 크게, 갤러리 썸네일은 작게) — 같은 원본을 크기만 바꿔
불러오므로 트래픽을 아낄 수 있다.

## 다음 단계

1. `components/header.html` 브랜드명을 "여러 강아지 소개" 컨셉에 맞게 변경
2. `css/style.css`를 흰 배경 + 단일 포인트 컬러로 재작성
3. `js/main.js`에 Swiper 초기화 로직 추가
4. `index.html`(스와이퍼 히어로), `profile.html`(5마리 카드), `gallery.html`(사진 그리드),
   `diary.html`(강아지별 에피소드) 작성
5. 로컬 서버로 확인 후 커밋/푸시
